# QualityDataModelPython
1. update poetry (aas2openapi sollte dann installiert sein, wenn nicht pip install aas2openapi ausführen)
2. docker compose build
3. docker mit docker compose up starten 
4. run aas2openapi-client.post_aas_put_kmg_sensor_data_test.py sollte eine test AAS postenund könnt ihr unter http://localhost:8000/QualityDataAAS/ aufrufen